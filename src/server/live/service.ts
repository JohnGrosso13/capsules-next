/**
 * Placeholder live/streaming domain service.
 * Flesh out with realtime stream state, chat orchestration, etc.
 */
export async function planLiveSession(): Promise<never> {
  throw new Error("Live domain not implemented yet");
}
